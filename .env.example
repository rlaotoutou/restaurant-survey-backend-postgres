# Railway will populate DATABASE_URL automatically when you add a Postgres database.
# Example: postgres://username:password@host:port/dbname
DATABASE_URL=

# Allowed CORS origin(s), comma-separated. Example:
# CORS_ORIGIN=https://your-frontend.example.com,https://another.site
CORS_ORIGIN=*

# Admin key to protect export/listing endpoints
ADMIN_KEY=change-me
